import React from 'react';

const SearchBar = () => {
    return (
        <div>
            <label>Enter term:</label>
            <br />
            <input type='text' />
            <button>Search</button>
        </div>
    );
};

export default SearchBar;